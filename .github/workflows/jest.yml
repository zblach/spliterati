name: Jest

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: get repo
        uses: actions/checkout@v2
      - name: yarn
        run: yarn
      - name: Run Tests
        run: jest test
